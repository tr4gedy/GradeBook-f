<Window x:Class="GradeBook.Views.GradesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Все оценки студента" Height="500" Width="650" 
        WindowStartupLocation="CenterOwner" FontSize="15">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333333"/>

        <DataGrid x:Name="GradesGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" SelectionMode="Single" RowHeight="36" 
                  GridLinesVisibility="Horizontal" Background="White" BorderThickness="1" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Предмет" Binding="{Binding Subject.Title}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" IsReadOnly="True" Width="130"/>
                <DataGridTextColumn Header="Оценка (2-5)" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Button Content="❌ Удалить оценку" Click="Delete_Click" 
                    HorizontalAlignment="Left" Padding="12,6" Background="#FFEBEE" BorderBrush="#FFCDD2" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Сохранить изменения" Click="Save_Click" 
                        Padding="15,6" Margin="0,0,10,0" Background="#E8F5E9" BorderBrush="#C8E6C9" FontWeight="SemiBold"/>
                <Button Content="Закрыть" Click="Close_Click" Padding="15,6"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>